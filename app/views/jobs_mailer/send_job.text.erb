<%= @job.title%>
<% if not @job.salary.blank? then %>
  <%= @job.salary%>
<% end %>
<%=HtmlToPlainText.plain_text(@job.description).squish.truncate(300, separator: ' ',omission: '') + "..." %>

<%=job_url(@job) %>
--------------------------------

<% @list_of_jobs.each do |job|%>
  <%= job.title %> (<%= job_url job%>)
  <%= job.company.name%>(<%= company_url job.company%>) - <%= job.location.name%>(<%= local_object_url(job.location.id,Objects::JOBS.code)%>)

<% end %>

<%= "#{ root_url } - More jobs in real time." %>

<%= render "inc_mailer/mailing"%>